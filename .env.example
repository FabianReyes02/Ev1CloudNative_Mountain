# URL base de la API (AWS API Gateway) cuando el backend esté conectado
VITE_API_BASE_URL=/api

# true = usa datos locales simulados (sin backend). Cambia a false al conectar microservicios.
VITE_USE_MOCK=true